World.Ship = Class.extend({
    x           : 0,
    y           : 0,
    angle       : 0,

    init : function (x, y) {
        this.x = x;
        this.y = y;
    },

    move : function (ship) {




    }

});
